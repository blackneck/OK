import { createAction } from 'redux-actions'

export const SET_TIME = createAction('SET_TIME')
export const SET_FREQUENCY = createAction('SET_FREQUENCY')
export const SET_NOTIFICATION_ID = createAction('SET_NOTIFICATION_ID')
